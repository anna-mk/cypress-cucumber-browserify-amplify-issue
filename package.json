{
  "name": "cypress-cucumber-browserify-amplify-issue",
  "version": "1.0.0",
  "scripts": {
    "test": "npx cypress run"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.1",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "aws-amplify": "^5.2.6",
    "cypress": "^12.14.0"
  }
}
